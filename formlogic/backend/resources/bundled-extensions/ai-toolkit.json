{
  "formatVersion": 2,
  "package": {
    "id": "com.formlogic.ai-toolkit",
    "kind": "extension",
    "version": "1.1.0",
    "publisherId": "com.formlogic",
    "displayName": "AI Toolkit",
    "description": "The LLM Chat, Speech to Text and Text to Speech flow nodes, delivered as a package so they can be updated without a release of the app.",
    "keywords": ["ai", "llm", "chat", "speech", "transcription", "text-to-speech", "audio"]
  },
  "contributions": {
    "flowNodes": [
      {
        "schemaVersion": 1,
        "type": "com.formlogic.ai.chat",
        "version": "1.1.0",
        "display": {
          "label": "LLM Chat",
          "description": "Call an OpenAI-compatible chat model and get its reply.",
          "category": "AI",
          "iconId": "sparkles"
        },
        "configurationSchema": {
          "type": "object",
          "properties": {
            "system": { "type": "string", "title": "System prompt", "description": "Sets the model's role. Supports {{...}} templating against the run scope." },
            "prompt": { "type": "string", "title": "Prompt", "description": "Supports {{...}} templating against the run scope." },
            "provider": { "type": "string", "title": "AI service", "description": "Leave on Auto to use the paired Desktop model or the app's configured provider." },
            "model": { "type": "string", "title": "Model", "description": "Optional." },
            "endpoint": { "type": "string", "title": "Endpoint", "description": "Optional OpenAI-compatible URL; blank uses the resolved service." },
            "temperature": { "type": "number", "title": "Temperature", "minimum": 0, "maximum": 2 },
            "maxTokens": { "type": "integer", "title": "Max tokens", "minimum": 1 }
          },
          "required": ["prompt"]
        },
        "uiHints": {
          "system": { "control": "textarea" },
          "prompt": { "control": "textarea" },
          "provider": { "control": "aiProvider" }
        },
        "handler": {
          "kind": "core-preset",
          "coreType": "llm_chat",
          "defaults": {}
        },
        "sideEffects": "external-write"
      },
      {
        "schemaVersion": 1,
        "type": "com.formlogic.ai.transcribe",
        "version": "1.1.0",
        "display": {
          "label": "Speech to Text",
          "description": "Transcribe audio to text.",
          "category": "AI",
          "iconId": "mic"
        },
        "configurationSchema": {
          "type": "object",
          "properties": {
            "audio": { "type": "string", "title": "Audio", "description": "A data URL, URL or base64 audio string — e.g. $inputs.recording." },
            "provider": { "type": "string", "title": "AI service", "description": "Leave on Auto to use the default Desktop speech service." },
            "model": { "type": "string", "title": "Model", "description": "Optional." },
            "endpoint": { "type": "string", "title": "Endpoint", "description": "Optional; blank uses the resolved speech service." },
            "service": { "type": "string", "title": "Service id", "description": "Optional Desktop service to resolve the endpoint from." }
          },
          "required": ["audio"]
        },
        "uiHints": {
          "provider": { "control": "aiProvider" },
          "service": { "control": "desktopService" }
        },
        "handler": {
          "kind": "core-preset",
          "coreType": "stt_transcribe",
          "defaults": {}
        },
        "sideEffects": "external-write"
      },
      {
        "schemaVersion": 1,
        "type": "com.formlogic.ai.speak",
        "version": "1.1.0",
        "display": {
          "label": "Text to Speech",
          "description": "Turn text into spoken audio.",
          "category": "AI",
          "iconId": "volume-2"
        },
        "configurationSchema": {
          "type": "object",
          "properties": {
            "text": { "type": "string", "title": "Text", "description": "What to say. Supports {{...}} templating against the run scope." },
            "provider": { "type": "string", "title": "AI service", "description": "Leave on Auto to use the default Desktop speech service." },
            "voice": { "type": "string", "title": "Voice", "description": "Optional." },
            "model": { "type": "string", "title": "Model", "description": "Optional." },
            "endpoint": { "type": "string", "title": "Endpoint", "description": "Optional; blank uses the resolved speech service." },
            "service": { "type": "string", "title": "Service id", "description": "Optional Desktop service to resolve the endpoint from." }
          },
          "required": ["text"]
        },
        "uiHints": {
          "text": { "control": "textarea" },
          "provider": { "control": "aiProvider" },
          "service": { "control": "desktopService" }
        },
        "handler": {
          "kind": "core-preset",
          "coreType": "tts_speak",
          "defaults": {}
        },
        "sideEffects": "external-write"
      }
    ]
  }
}
